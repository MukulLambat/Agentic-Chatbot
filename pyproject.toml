[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "langchain-chatbot"
version = "0.1.0"
description = "A chatbot built with LangChain"
requires-python = ">=3.9"
